[project]
name = "brickcollectionmanager"
version = "0.3.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python-headless>=4.12.0.88",
    "pyside6>=6.9.1",
    "qt-material>=2.17",
    "requests>=2.32.5",
]

[project.optional-dependencies]
build = [
    "pillow>=10.0.0",  # For icon conversion during build
]

[dependency-groups]
dev = [
    "pillow>=11.3.0",
    "pyinstaller>=6.16.0",
]
